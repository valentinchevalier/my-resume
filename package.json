{
  "name": "my-resume",
  "version": "1.0.0",
  "description": "My resume, written in `.pug` and `.scss`",
  "author": "Valentin Chevalier <valche.vc@gmail.com>",
  "main": "",
  "scripts": {
    "start": "relaxed resume.pug",
    "icon:generate": "icon-font-generator icons/*.svg -o dist",
    "build": "relaxed resume.pug --build-once"
  },
  "dependencies": {
    "icon-font-generator": "^2.1.10",
    "relaxedjs": "^0.2.3"
  }
}
